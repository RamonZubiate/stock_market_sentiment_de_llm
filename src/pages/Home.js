import React from "react";

const Home = () => {
  return <h2 style={styles.page}>🏠 Home Page</h2>;
};

const styles = {
  page: {
    fontSize: "24px",
    fontWeight: "bold",
    textAlign: "center",
    marginTop: "20px",
  },
};

export default Home;
